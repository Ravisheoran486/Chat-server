services:
  - type: web
    name: websocket-chat
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python server/chat_server.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0
    healthCheckPath: /health 